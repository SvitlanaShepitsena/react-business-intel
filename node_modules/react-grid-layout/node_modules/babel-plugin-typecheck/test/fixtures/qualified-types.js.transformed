"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

var T = {
  Object: Object,
  Array: Array
};

function demo(foo) {
  if (!(foo instanceof T.Object || foo instanceof T.Array)) {
    throw new TypeError("Value of argument \"foo\" violates contract, expected T.Object | T.Array got " + (foo === null ? 'null' : (typeof foo === "undefined" ? "undefined" : _typeof(foo)) === 'object' && foo.constructor ? foo.constructor.name || '[Unknown Object]' : typeof foo === "undefined" ? "undefined" : _typeof(foo)));
  }

  return true;
}