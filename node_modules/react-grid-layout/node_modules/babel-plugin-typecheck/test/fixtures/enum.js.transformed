"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

var status = function status(input) {
  return input === "active" || input === "inactive";
};

function demo(input) {
  if (!status(input)) {
    throw new TypeError("Value of argument \"input\" violates contract, expected status got " + (input === null ? 'null' : (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && input.constructor ? input.constructor.name || '[Unknown Object]' : typeof input === "undefined" ? "undefined" : _typeof(input)));
  }

  if (status("active")) {
    return "active";
  } else {
    throw new TypeError("Function \"demo\" return value violates contract, expected status got " + ("active" === null ? 'null' : _typeof("active") === 'object' && "active".constructor ? "active".constructor.name || '[Unknown Object]' : _typeof("active")));
  }
}