"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

var Thing = function Thing(input) {
  return input != null && (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && typeof input.name === 'string';
};

;

var Person = function Person(input) {
  return input != null && (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && typeof input.age === 'number';
};

;

var User = function User(input) {
  _classCallCheck(this, User);

  if (!(input instanceof Object)) {
    throw new TypeError("Value of argument \"input\" violates contract, expected Object got " + (input === null ? 'null' : (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && input.constructor ? input.constructor.name || '[Unknown Object]' : typeof input === "undefined" ? "undefined" : _typeof(input)));
  }

  this.input = input;
};

function demo(input) {
  if (!(input instanceof Object)) {
    throw new TypeError("Value of argument \"input\" violates contract, expected Object got " + (input === null ? 'null' : (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && input.constructor ? input.constructor.name || '[Unknown Object]' : typeof input === "undefined" ? "undefined" : _typeof(input)));
  }

  return new User(input);
}