"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

var User = exports.User = function User(input) {
  return input != null && (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && typeof input.name === 'string';
};

function demo(input) {
  var _output$name;

  if (!User(input)) {
    throw new TypeError("Value of argument \"input\" violates contract, expected User got " + (input === null ? 'null' : (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && input.constructor ? input.constructor.name || '[Unknown Object]' : typeof input === "undefined" ? "undefined" : _typeof(input)));
  }

  var output = {
    name: "test",
    address: "123 Fake Street"
  };
  _output$name = output.name;

  if (typeof _output$name === 'string') {
    return _output$name;
  } else {
    throw new TypeError("Function \"demo\" return value violates contract, expected string got " + (_output$name === null ? 'null' : (typeof _output$name === "undefined" ? "undefined" : _typeof(_output$name)) === 'object' && _output$name.constructor ? _output$name.constructor.name || '[Unknown Object]' : typeof _output$name === "undefined" ? "undefined" : _typeof(_output$name)));
  }
}