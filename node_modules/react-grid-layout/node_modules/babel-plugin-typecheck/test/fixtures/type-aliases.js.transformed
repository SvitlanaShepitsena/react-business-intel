"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

var Blob = function Blob(input) {
  return input instanceof Buffer;
};

var integer = function integer(input) {
  return typeof input === 'number';
};

var str = function str(input) {
  return typeof input === 'string';
};

var strOrNumber = function strOrNumber(input) {
  return typeof input === 'string' || typeof input === 'number';
};

var arr = function arr(input) {
  return Array.isArray(input) && input.length >= 3 && typeof input[0] === 'string' && integer(input[1]) && Blob(input[2]);
};

var strings = function strings(input) {
  return Array.isArray(input) && input.every(function (item) {
    return typeof item === 'string' || typeof item === 'number' || Blob(item) || item instanceof Buffer;
  });
};

var obj = function obj(input) {
  return input != null && (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && typeof input.name === 'string' && typeof input.age === 'number' && input.location != null && _typeof(input.location) === 'object' && arr(input.location.city) && typeof input.location.blub === 'function';
};

function demo(value, extra, wat) {
  var _ref;

  if (!(typeof extra === 'string')) {
    throw new TypeError("Value of argument \"extra\" violates contract, expected string got " + (extra === null ? 'null' : (typeof extra === "undefined" ? "undefined" : _typeof(extra)) === 'object' && extra.constructor ? extra.constructor.name || '[Unknown Object]' : typeof extra === "undefined" ? "undefined" : _typeof(extra)));
  }

  if (!(wat != null && (typeof wat === "undefined" ? "undefined" : _typeof(wat)) === 'object' && typeof wat.foo === 'string' && (typeof wat.bar === 'number' || Array.isArray(wat.bar) && wat.bar.every(function (item) {
    return typeof item === 'string';
  })))) {
    throw new TypeError("Value of argument \"wat\" violates contract, expected { foo: string;\n  bar: number | Array<string>;\n} got " + (wat === null ? 'null' : (typeof wat === "undefined" ? "undefined" : _typeof(wat)) === 'object' && wat.constructor ? wat.constructor.name || '[Unknown Object]' : typeof wat === "undefined" ? "undefined" : _typeof(wat)));
  }

  var someValue = "123";
  _ref = value + someValue;

  if (typeof _ref === 'string') {
    return _ref;
  } else {
    throw new TypeError("Function \"demo\" return value violates contract, expected T | string got " + (_ref === null ? 'null' : (typeof _ref === "undefined" ? "undefined" : _typeof(_ref)) === 'object' && _ref.constructor ? _ref.constructor.name || '[Unknown Object]' : typeof _ref === "undefined" ? "undefined" : _typeof(_ref)));
  }
}