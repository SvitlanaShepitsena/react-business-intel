"use strict";

Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.default = test;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

function zero() {
	var _length;

	if (true) {
			_length = [].length;

			if (typeof _length === 'number') {
				return _length;
			} else {
				throw new TypeError("Function \"zero\" return value violates contract, expected number got " + (_length === null ? 'null' : (typeof _length === "undefined" ? "undefined" : _typeof(_length)) === 'object' && _length.constructor ? _length.constructor.name || '[Unknown Object]' : typeof _length === "undefined" ? "undefined" : _typeof(_length)));
			}
		}
}

function test() {
	return zero();
}