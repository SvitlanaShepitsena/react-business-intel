"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

function demo(opt) {
  if (!(opt == null || opt instanceof Object)) {
    throw new TypeError("Value of argument \"opt\" violates contract, expected ?Object got " + (opt === null ? 'null' : (typeof opt === "undefined" ? "undefined" : _typeof(opt)) === 'object' && opt.constructor ? opt.constructor.name || '[Unknown Object]' : typeof opt === "undefined" ? "undefined" : _typeof(opt)));
  }

  if (opt) {
    var _identity;

    _identity = identity(opt);

    if (typeof _identity === 'number' || typeof _identity === 'string') {
      return _identity;
    } else {
      throw new TypeError("Function \"demo\" return value violates contract, expected number | string got " + (_identity === null ? 'null' : (typeof _identity === "undefined" ? "undefined" : _typeof(_identity)) === 'object' && _identity.constructor ? _identity.constructor.name || '[Unknown Object]' : typeof _identity === "undefined" ? "undefined" : _typeof(_identity)));
    }
  } else if (Math.random() > 0.3) {
    var _stringGen;

    _stringGen = stringGen();

    if (typeof _stringGen === 'number' || typeof _stringGen === 'string') {
      return _stringGen;
    } else {
      throw new TypeError("Function \"demo\" return value violates contract, expected number | string got " + (_stringGen === null ? 'null' : (typeof _stringGen === "undefined" ? "undefined" : _typeof(_stringGen)) === 'object' && _stringGen.constructor ? _stringGen.constructor.name || '[Unknown Object]' : typeof _stringGen === "undefined" ? "undefined" : _typeof(_stringGen)));
    }
  } else {
    var _numberGen;

    _numberGen = numberGen();

    if (typeof _numberGen === 'number' || typeof _numberGen === 'string') {
      return _numberGen;
    } else {
      throw new TypeError("Function \"demo\" return value violates contract, expected number | string got " + (_numberGen === null ? 'null' : (typeof _numberGen === "undefined" ? "undefined" : _typeof(_numberGen)) === 'object' && _numberGen.constructor ? _numberGen.constructor.name || '[Unknown Object]' : typeof _numberGen === "undefined" ? "undefined" : _typeof(_numberGen)));
    }
  }
}

function stringGen() {
  return "test";
}

function numberGen() {
  return 123;
}

function identity(input) {
  return input;
}